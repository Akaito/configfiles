" Stop continuing comments with o/O in *.vim files.
setlocal formatoptions-=o

" Note that if we wanted to remove multiple options, we'd have to remove one
" option at a time:
"setlocal formatoptions-=o fo-=a

